#!/usr/bin/env bash
# Unix compatibility launcher; portable behavior lives in codeops_worktree.py.
set -euo pipefail
BIN_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$BIN_DIR/../scripts/codeops_worktree.py" "$@"
